import 'dart:io';

class Globals {
  static String? name, email, contact, address, password, gender, dob;
  static File? image;
  static bool isChecked = false;
  static List<String> languages = [];
}
